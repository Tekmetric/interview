const colorTokens = {
  palePink: '#E0D5D9',
  pink: '#a082c1',
  dustyViolet: '#705E84',
  white: '#FFFFFF',
};

const colors = {
  hover: colorTokens.palePink,
  active: colorTokens.dustyViolet,
  scrollbarColor: colorTokens.palePink,
  scrollbarBackGdColor: colorTokens.white,
  headerColor: colorTokens.pink,
  background: colorTokens.white,
  headerText: colorTokens.white,
};

const spacing = {
  s: '8px',
  m: '16px',
  l: '32px',
  xl: '48px',
};

const opacity = {
  background: 0.8,
  backgroundLight: 0.6,
};

const fontSize = {
  s: '14px',
  m: '20px',
};

export const lightTheme = {
  opacity,
  spacing,
  colors,
  fontSize,
};

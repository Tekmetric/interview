package com.interview.runningevents.application;

/**
 * The application layer coordinates the execution of business logic.
 * It acts as a facade between the domain layer and the infrastructure layer.
 *
 * Key components include:
 * - Use cases / application services
 * - DTOs (Data Transfer Objects)
 * - Command and query handlers
 */

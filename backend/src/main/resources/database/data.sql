DROP TABLE IF EXISTS work_orders;

CREATE TABLE work_orders (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    customer_name VARCHAR(120) NOT NULL,
    vin VARCHAR(17) NOT NULL,
    issue_description VARCHAR(500) NOT NULL,
    status VARCHAR(30) NOT NULL,
    created_at TIMESTAMP NOT NULL,
    updated_at TIMESTAMP NOT NULL
);

INSERT INTO work_orders (customer_name, vin, issue_description, status, created_at, updated_at)
VALUES ('Alice Johnson', '1HGCM82633A004352', 'Brake pads replacement', 'OPEN', CURRENT_TIMESTAMP, CURRENT_TIMESTAMP);

INSERT INTO work_orders (customer_name, vin, issue_description, status, created_at, updated_at)
VALUES ('Marcus Lee', 'JH4KA9650MC012345', 'Check engine light diagnosis', 'IN_PROGRESS', CURRENT_TIMESTAMP, CURRENT_TIMESTAMP);

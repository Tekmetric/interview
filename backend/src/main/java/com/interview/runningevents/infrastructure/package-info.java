package com.interview.runningevents.infrastructure;

/**
 * The infrastructure layer contains implementations of interfaces defined in the domain layer
 * and provides technical capabilities to support the higher layers.
 *
 * Key components include:
 * - Repository implementations (adapters)
 * - Database configurations
 * - External service clients
 * - Web controllers
 * - Security configurations
 */

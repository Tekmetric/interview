config:
  target: 'http://app:8080'
  phases:
    - duration: 60
      arrivalRate: 3
      name: "Docker environment test"
  defaults:
    headers:
      Authorization: 'Basic YWRtaW46YWRtaW4xMjM='
      Content-Type: 'application/json'

scenarios:
  - name: "Docker API Test"
    flow:
      - get:
          url: "/actuator/health"
      - get:
          url: "/api/products/active"
      - get:
          url: "/api/inventory/low-stock?threshold=20"
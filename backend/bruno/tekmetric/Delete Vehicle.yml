info:
  name: Delete Vehicle
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:8080/api/vehicles/4
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

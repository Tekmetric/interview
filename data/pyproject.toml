[tool.poetry]
name = "data"
version = "0.1.0"
description = ""
authors = ["Sebastian Ortan <dynamic.mindset@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
requests = "^2.32.3"
pyarrow = "^19.0.1"
requests-cache = "^1.2.1"


[tool.poetry.group.dev.dependencies]
black = "^25.1.0"
pre-commit = "^4.1.0"
pytest = "^8.3.4"
python-dotenv = "^1.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
neo_collect = "data.main:main"

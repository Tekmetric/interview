global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  # Scrape the Spring Boot carshop app running on the host machine.
  - job_name: carshop
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - host.docker.internal:8080

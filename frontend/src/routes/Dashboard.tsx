import React from "react";
import { Typography } from "@mui/material";
import { MainContent } from "../components/Layout";

export const Dashboard = () => {
  return (
    <MainContent>
      <Typography variant="subtitle1">Dashboard - TBD</Typography>
    </MainContent>
  );
};

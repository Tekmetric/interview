import styled from 'styled-components';

export const StyledArtworkImageSpace = styled.div`
    position: relative;
    display: flex;
    flex-shrink: 0;
    justify-content: center;
    align-items: center;
    width: 100vw;
    height: 100%;
    scroll-snap-align: start;
    background: radial-gradient(circle at 50% 0, #eee 4%, #c5c5c5 50%);
`

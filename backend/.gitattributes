#Set default behavior to automatically normalize line endings
* text=auto

# Explicitly declare text files to be normalized
*.java text
*.xml text
*.properties text

#Declare binary files to be left unchanged
*.png binary
*.jpg binary
*.gif binary

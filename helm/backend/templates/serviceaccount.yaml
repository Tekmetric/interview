{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "backend.fullname" .) .Values.serviceAccount.name }}
  labels:
    {{- include "backend.labels" . | nindent 4 }}
{{- end -}}


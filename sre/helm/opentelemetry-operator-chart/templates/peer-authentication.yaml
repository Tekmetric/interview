apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: {{ include "opentelemetry-operator-chart.fullname" . }}-mtls
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "opentelemetry-operator-chart.labels" . | nindent 4 }}
spec:
  mtls:
    mode: PERMISSIVE

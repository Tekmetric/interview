-- Reset table so the app/test database starts from a known state.
DROP TABLE IF EXISTS repair_orders;

-- Single-table schema for the repair order CRUD exercise.
CREATE TABLE repair_orders
(
    id            BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    customer_name VARCHAR(100)   NOT NULL,
    vehicle_vin   VARCHAR(17)    NOT NULL UNIQUE,
    description   VARCHAR(500)   NOT NULL,
    status        VARCHAR(30)    NOT NULL,
    total_cost    DECIMAL(10, 2) NOT NULL,
    created_at    TIMESTAMP      NOT NULL,
    updated_at    TIMESTAMP      NOT NULL
);

-- Seed records used for manual API testing and integration tests.
INSERT INTO repair_orders (customer_name, vehicle_vin, description, status, total_cost, created_at, updated_at)
VALUES ('Jane Doe', '1HGCM82633A004352', 'Brake pad replacement and rotor inspection', 'OPEN', 325.00,
        CURRENT_TIMESTAMP, CURRENT_TIMESTAMP),
       ('John Smith', '5YJSA1E14HF123456', 'Oil change and tire rotation', 'IN_PROGRESS', 89.99, CURRENT_TIMESTAMP,
        CURRENT_TIMESTAMP);

import React from 'react';
import { Button } from '@mui/material';

const App = () => (
  <div className="App">
    <Button variant="contained" color="primary">
      Hello, Material-UI!
    </Button>
  </div>
);

export default App;

spring:
  application:
    name: interview
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none

logging:
  level:
    org.springframework.web.filter.CommonsRequestLoggingFilter: DEBUG #just for demo/debugging would turn off in a prod env
